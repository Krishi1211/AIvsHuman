# AIvsHuman

The final calibrated study workflow now lives in `GPTSnifferInferenceApproach/`.

Start here:
- `GPTSnifferInferenceApproach/README.md` for the full reproducible pipeline and results
- `README.legacy-airflow-case-study.md` for the older repo-level README kept for reference

Legacy components kept at the repo root:
- `GPTSniffer/`
- `Pydriller/`
- `detect-gpt-fork/`
